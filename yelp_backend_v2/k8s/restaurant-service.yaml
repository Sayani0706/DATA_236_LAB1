apiVersion: v1
kind: Service
metadata:
  name: restaurant-service
  namespace: yelp-lab2
spec:
  selector:
    app: restaurant-service
  ports:
    - protocol: TCP
      port: 8002
      targetPort: 8002
  type: ClusterIP
